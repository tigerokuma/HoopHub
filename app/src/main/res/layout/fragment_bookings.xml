<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Full-Screen Background Image -->
    <ImageView
        android:id="@+id/backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/chat_background"
        android:alpha="0.3" />

    <!-- RecyclerView for booking list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/bookingRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="85dp"
        android:background="@android:color/transparent" /> <!-- Made background transparent -->

    <!-- Floating action button for adding games -->
    <Button
        android:id="@+id/addGameFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:elevation="8dp"
        android:layout_marginTop="24dp"
        android:layout_marginRight="20dp"
        app:backgroundTint="@color/highlight"
        android:tint="@color/white"
        android:text="Add Game" />

    <!-- Placeholder text before RecyclerView -->
    <TextView
        android:id="@+id/placeholderText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Your scheduled games will appear here."
        android:textColor="@color/highlight"
        android:textStyle="bold"
        android:textSize="15dp"
        android:gravity="center"
        android:layout_marginTop="80dp"
        android:background="@color/white"
        android:padding="8dp"
        android:elevation="4dp" />

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">


    </androidx.cardview.widget.CardView>

</FrameLayout>
